# Editor/OS
.DS_Store
.idea/
.vscode/
.claude/

# Python
__pycache__/
*.pyc
*.pyo

# Secrets (nunca commitar)
.env

# Testes locais (nao fazem parte do deploy)
local-tests/

# Docker compose local (apenas para desenvolvimento)
docker-compose.local.yml

# Workflows n8n exportados (versionar separadamente se necessario)
/workflows/

# Backups do banco baixados via GET /backup (nunca commitar — dados de producao)
/backup-bd/
